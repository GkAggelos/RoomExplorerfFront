import { AllreadExistsValidatorDirective } from './allread-exists-validator.directive';

describe('AllreadExistsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AllreadExistsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
